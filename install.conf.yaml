- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim 
    ~/.zshrc: zshrc

- create:
    - ~/work
    - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
 
