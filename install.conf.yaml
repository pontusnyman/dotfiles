- defaults:
    link:
      relink: true

- clean: ['~']

- link: 
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/work
    - ~/.vim/undo-history

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
   - echo "hey you guys"
   - sleep 5
