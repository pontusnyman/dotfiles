- defaults:
    link:
      relink: true

- clean: ['~']

- link: 
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/work
    - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
 
